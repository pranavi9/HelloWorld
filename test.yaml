- execute:
          - invoke:
              cache-response: protocol
              cache-ttl: 900
              description: IIB Get Map Rest
              password: $(ApicPassword)
              stop-on-error:
              - null
              target-url: $(easypayApiBase)\getmap
              timeout: 60
              title: invokeGetMap
              username: $(ApicUser)
              verb: POST
              version: 1.0.0
          operations:
            - invoke:
              cache-response: protocol
              cache-ttl: 900
              stop-on-error:
              []
              target-url: $(iibendpoint)
              timeout: 60
              title: invoke
              verb: keep
              version: 1.0.0